supabase
python-dotenv
pymupdf
tiktoken
numpy
fastapi
uvicorn
#streamlit
pytest

# Local (free, offline) embeddings - EMBEDDING_PROVIDER=local
#torch
#transformers

# OCR for scanned PDFs (no system Tesseract needed)
# rapidocr-onnxruntime

# OpenAI-compatible client: used for LLM_PROVIDER=openai and for
# LLM_PROVIDER=local talking to Ollama / LM Studio
openai

# Optional: EMBEDDING_PROVIDER=gemini or LLM_PROVIDER=gemini (free tier)
# google-genai
