# Backend API base (no trailing slash). Leave empty to use Vite proxy `/api`.
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to force mock responses even if API is configured.
VITE_USE_MOCK_API=true
